[package]
name = "disposables"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = {version = "1", features = ["derive"]}
serde_json = "1"
thiserror = "1"

disposables_protocol = {path = "../protocol"}

[dev-dependencies]
log = "0.4.22"
env_logger = "0.11.5"
